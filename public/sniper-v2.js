(function(root){
const C={period:20,minHistory:205,minRR:1,minBodyAtr:.18,maxExtAtr:3.2,zoneAtr:.35,retestAtr:.55,reversalWickBody:.85,tacticalLookback:12,minTFAligned:3,minRiskAtr:.60,minCloseLocation:.62,minBreakAtr:.15,tp2MinBodyAtr:1.15,tp2MinClose:.72,buyScoreMin:60,sellScoreMax:40};
const sma=(v,p)=>v.length<p?null:v.slice(-p).reduce((a,b)=>a+b,0)/p;
function ema(v,p){if(v.length<p)return null;const k=2/(p+1);let x=v[0];for(let i=1;i<v.length;i++)x=v[i]*k+x*(1-k);return x}
function rsi(v,p=14){if(v.length<p+1)return null;let g=0,l=0;for(let i=v.length-p;i<v.length;i++){const d=v[i]-v[i-1];if(d>=0)g+=d;else l-=d}return l===0?100:100-100/(1+g/l)}
function atr(c,p=14){if(c.length<p+1)return null;const r=[];for(let i=1;i<c.length;i++){const x=c[i],q=c[i-1].close;r.push(Math.max(x.high-x.low,Math.abs(x.high-q),Math.abs(x.low-q)))}return sma(r,p)}
function trend(c){const v=c.map(x=>x.close),f=ema(v,20),s=ema(v,50);if(f==null||s==null)return'LATERAL';const last=v.at(-1),prev=v.at(-4);if(last>f&&f>=s&&f>=prev)return'ALTA';if(last<f&&f<=s&&f<=prev)return'BAIXA';return last>f?'ALTA':last<f?'BAIXA':'LATERAL'}
function structure(c,p=C.period){const x=c.slice(-(p+1),-1);return x.length<p?null:{support:Math.min(...x.map(a=>a.low)),resistance:Math.max(...x.map(a=>a.high))}}
function round(v){return Number(v.toFixed(2))}
function rejection(sig){const body=Math.abs(sig.close-sig.open),range=Math.max(sig.high-sig.low,1e-9),lower=Math.min(sig.open,sig.close)-sig.low,upper=sig.high-Math.max(sig.open,sig.close);return{bull:sig.close>sig.open&&lower>=Math.max(body*C.reversalWickBody,range*.20)&&sig.close>=sig.low+range*.55,bear:sig.close<sig.open&&upper>=Math.max(body*C.reversalWickBody,range*.20)&&sig.close<=sig.high-range*.55}}
function structureSignal(c,p=12){const x=c.slice(-(p+1));if(x.length<p+1)return{bosBuy:false,bosSell:false,chochBuy:false,chochSell:false};const last=x.at(-1),prior=x.slice(0,-1),hi=Math.max(...prior.map(v=>v.high)),lo=Math.min(...prior.map(v=>v.low));const mid=Math.floor(prior.length/2),left=prior.slice(0,mid),right=prior.slice(mid);const leftHi=Math.max(...left.map(v=>v.high)),rightHi=Math.max(...right.map(v=>v.high)),leftLo=Math.min(...left.map(v=>v.low)),rightLo=Math.min(...right.map(v=>v.low));return{bosBuy:last.close>hi,bosSell:last.close<lo,chochBuy:last.close>rightHi&&rightLo<leftLo,chochSell:last.close<rightLo&&rightHi>leftHi}}
function recentEvent(c,dir,lookback=C.tacticalLookback){const end=c.length-1,start=Math.max(C.period,end-lookback);for(let i=end-1;i>=start;i--){const before=c.slice(Math.max(0,i-C.period),i);if(before.length<C.period)continue;const level=dir==='BUY'?Math.max(...before.map(x=>x.high)):Math.min(...before.map(x=>x.low));const prev=c[i-1],x=c[i];if(dir==='BUY'&&x.close>level&&prev.close<=level)return{index:i,level,type:'BREAKOUT'};if(dir==='SELL'&&x.close<level&&prev.close>=level)return{index:i,level,type:'BREAKDOWN'}}return null}
function tactical(c,dir,S,A,sig,prev){const rej=rejection(sig),tol=A*C.zoneAtr,level=dir==='BUY'?S.resistance:S.support;const direct=dir==='BUY'?sig.close>level&&prev.close<=level:sig.close<level&&prev.close>=level;const near=dir==='BUY'?sig.low<=level+tol&&sig.close>level&&rej.bull:sig.high>=level-tol&&sig.close<level&&rej.bear;const ev=recentEvent(c,dir);let retest=false,continuation=false,retestIndex=null,confirmationIndex=null;if(ev&&ev.index<c.length-1){const after=c.slice(ev.index+1,-1);if(after.length){const touchTol=A*C.retestAtr;if(dir==='BUY'){for(let j=0;j<after.length;j++){const p=after[j],touched=p.low<=ev.level+touchTol&&p.low>=ev.level-touchTol,held=p.close>=ev.level-touchTol;if(!touched||!held)continue;const next=after[j+1];if(next){const nextRej=rejection(next),confirmed=sig.close>next.high&&sig.close>ev.level&&nextRej.bull,currentRetest=sig.low<=ev.level+touchTol&&sig.close>ev.level&&rej.bull&&sig.close>p.high;retest=confirmed||currentRetest;if(retest){retestIndex=ev.index+1+j;confirmationIndex=ev.index+2+j;break}}else{const confirmed=sig.close>p.high&&sig.close>ev.level&&rej.bull;retest=confirmed;if(retest){retestIndex=ev.index+1+j;confirmationIndex=ev.index+1+j;break}}}const priorHigh=Math.max(...after.map(x=>x.high)),held=after.every(x=>x.close>=ev.level-touchTol);continuation=held&&sig.close>priorHigh&&sig.close>ev.level}else{for(let j=0;j<after.length;j++){const p=after[j],touched=p.high>=ev.level-touchTol&&p.high<=ev.level+touchTol,held=p.close<=ev.level+touchTol;if(!touched||!held)continue;const next=after[j+1];if(next){const nextRej=rejection(next),confirmed=sig.close<next.low&&sig.close<ev.level&&nextRej.bear,currentRetest=sig.high>=ev.level-touchTol&&sig.close<ev.level&&rej.bear&&sig.close<p.low;retest=confirmed||currentRetest;if(retest){retestIndex=ev.index+1+j;confirmationIndex=ev.index+2+j;break}}else{const confirmed=sig.close<p.low&&sig.close<ev.level&&rej.bear;retest=confirmed;if(retest){retestIndex=ev.index+1+j;confirmationIndex=ev.index+1+j;break}}}const priorLow=Math.min(...after.map(x=>x.low)),held=after.every(x=>x.close<=ev.level+touchTol);continuation=held&&sig.close<priorLow&&sig.close<ev.level}}}return{direct,near,retest,continuation,event:ev,level,retestIndex,confirmationIndex}}
function strongMove(sig,A,dir){const body=Math.abs(sig.close-sig.open),range=Math.max(sig.high-sig.low,1e-9),closePos=(sig.close-sig.low)/range;return body>=A*C.tp2MinBodyAtr&&(dir==='BUY'?closePos>=C.tp2MinClose:closePos<=1-C.tp2MinClose)}
function makeTargets(entry,sl,dir,sig,A,tb,ts){const risk=Math.abs(entry-sl);if(!Number.isFinite(risk)||risk<=0)return{tp1:null,tp2:null,rr1:null,rr2:null,tp2Eligible:false};const tp1=dir==='BUY'?entry+risk:entry-risk,tacticalContinuation=dir==='BUY'?tb.continuation:ts.continuation,tacticalBreak=dir==='BUY'?tb.direct:ts.direct,strong=strongMove(sig,A,dir)&&(tacticalContinuation||tacticalBreak||(dir==='BUY'?tb.retest:ts.retest)),tp2=strong?(dir==='BUY'?entry+risk*2:entry-risk*2):null;return{tp1,tp2,rr1:1,rr2:strong?2:null,tp2Eligible:strong}}
function directionalScore({t5,t15,t1,entry,e200,R,tacticalBuy,tacticalSell,bodyOK,closeQualityBuy,closeQualitySell,atrOK}){let score=50;if(t5==='ALTA')score+=10;else if(t5==='BAIXA')score-=10;if(t15==='ALTA')score+=10;else if(t15==='BAIXA')score-=10;if(t1==='ALTA')score+=10;else if(t1==='BAIXA')score-=10;if(entry>e200)score+=8;else if(entry<e200)score-=8;if(R>50)score+=8;else if(R<50)score-=8;if(tacticalBuy&&!tacticalSell)score+=8;else if(tacticalSell&&!tacticalBuy)score-=8;if(bodyOK&&closeQualityBuy&&!closeQualitySell)score+=3;else if(bodyOK&&closeQualitySell&&!closeQualityBuy)score-=3;if(!atrOK)score=50;return Math.max(0,Math.min(100,Math.round(score)))}
function analyze(main,m5,m15,h1){if(![main,m5,m15,h1].every(Array.isArray)||main.length<C.minHistory||m5.length<55||m15.length<55||h1.length<55)return null;const closed=main.slice(0,-1),sig=closed.at(-1),prev=closed.at(-2),forming=main.at(-1),cl=closed.map(x=>x.close),entry=sig.close,e20=ema(cl,20),e50=ema(cl,50),e200=ema(cl,200),R=rsi(cl),A=atr(closed),S=structure(closed);if([e20,e50,e200,R,A].some(x=>x==null)||!S)return null;const t5=trend(m5.slice(0,-1)),t15=trend(m15.slice(0,-1)),t1=trend(h1.slice(0,-1)),ms=structureSignal(closed),tb=tactical(closed,'BUY',S,A,sig,prev),ts=tactical(closed,'SELL',S,A,sig,prev),bull=[t5,t15,t1].filter(x=>x==='ALTA').length,bear=[t5,t15,t1].filter(x=>x==='BAIXA').length,buyTF=bull>=C.minTFAligned,sellTF=bear>=C.minTFAligned,body=Math.abs(sig.close-sig.open),range=Math.max(sig.high-sig.low,1e-9),bodyOK=body>=A*C.minBodyAtr,closePos=(sig.close-sig.low)/range,closeQualityBuy=closePos>=C.minCloseLocation,closeQualitySell=closePos<=1-C.minCloseLocation,emaBuy=entry>e200,emaSell=entry<e200,extensionBuy=(entry-e20)/Math.max(A,1e-9)<=C.maxExtAtr,extensionSell=(e20-entry)/Math.max(A,1e-9)<=C.maxExtAtr,directBuyQuality=!tb.direct||((sig.close-tb.level)>=A*C.minBreakAtr&&closeQualityBuy),directSellQuality=!ts.direct||((ts.level-sig.close)>=A*C.minBreakAtr&&closeQualitySell),buyRSI=R>50&&R<=72,sellRSI=R>=28&&R<50,atrOK=A/Math.max(entry,1)>=.000015&&A/Math.max(entry,1)<=.012,tacticalBuy=tb.retest&&closeQualityBuy?'PULLBACK/RETESTE + REJEIÇÃO':tb.direct&&directBuyQuality?'BREAKOUT FECHADO':tb.continuation&&closeQualityBuy?'BREAKOUT + CONTINUAÇÃO':tb.near&&closeQualityBuy?'REJEIÇÃO/DEFESA DA RESISTÊNCIA':null,tacticalSell=ts.retest&&closeQualitySell?'RETESTE + REJEIÇÃO':ts.direct&&directSellQuality?'BREAKDOWN FECHADO':ts.continuation&&closeQualitySell?'BREAKDOWN + CONTINUAÇÃO':ts.near&&closeQualitySell?'REJEIÇÃO/DEFESA DO SUPORTE':null,bosBuyConfirm=ms.bosBuy||ms.chochBuy,bosSellConfirm=ms.bosSell||ms.chochSell,buyStructuralConfirm=bosBuyConfirm||!!tb.direct||!!tb.retest||!!tb.continuation,sellStructuralConfirm=bosSellConfirm||!!ts.direct||!!ts.retest||!!ts.continuation,buySetup=buyTF&&emaBuy&&extensionBuy&&buyRSI&&atrOK&&!!tacticalBuy&&buyStructuralConfirm,sellSetup=sellTF&&emaSell&&extensionSell&&sellRSI&&atrOK&&!!tacticalSell&&sellStructuralConfirm,bothSetup=buySetup&&sellSetup,type=!bothSetup&&bodyOK&&buySetup?'BUY':!bothSetup&&bodyOK&&sellSetup?'SELL':'WAIT',score=directionalScore({t5,t15,t1,entry,e200,R,tacticalBuy,tacticalSell,bodyOK,closeQualityBuy,closeQualitySell,atrOK}),scorePass=type==='BUY'?score>C.buyScoreMin:type==='SELL'?score<C.sellScoreMax:false,finalType=scorePass?type:'WAIT';let sl=null,tp1=null,tp2=null,rr1=null,rr2=null,rr=null,tp2Eligible=false;if(finalType==='BUY'){const tacticalLow=Math.min(sig.low,S.support);sl=Math.min(tacticalLow-A*.20,e20-A*.15);const targets=makeTargets(entry,sl,'BUY',sig,A,tb,ts);tp1=targets.tp1;tp2=targets.tp2;rr1=targets.rr1;rr2=targets.rr2;tp2Eligible=targets.tp2Eligible;rr=rr1}if(finalType==='SELL'){const tacticalHigh=Math.max(sig.high,S.resistance);sl=Math.max(tacticalHigh+A*.20,e20+A*.15);const targets=makeTargets(entry,sl,'SELL',sig,A,tb,ts);tp1=targets.tp1;tp2=targets.tp2;rr1=targets.rr1;rr2=targets.rr2;tp2Eligible=targets.tp2Eligible;rr=rr1}const riskDistance=Number.isFinite(entry)&&Number.isFinite(sl)?Math.abs(entry-sl):NaN;const riskAtr=Number.isFinite(riskDistance)&&Number.isFinite(A)&&A>0?riskDistance/A:NaN;const validRisk=Number.isFinite(rr1)&&rr1>=C.minRR&&Number.isFinite(tp1)&&Number.isFinite(sl)&&Number.isFinite(riskAtr)&&riskAtr>=C.minRiskAtr,outType=validRisk?finalType:'WAIT',directionalPass=outType==='BUY'?buyTF&&emaBuy&&extensionBuy&&buyRSI:outType==='SELL'?sellTF&&emaSell&&extensionSell&&sellRSI:false,setupPass=outType==='BUY'?buyStructuralConfirm:outType==='SELL'?sellStructuralConfirm:!!(tacticalBuy||tacticalSell),qualityParts=[atrOK,bodyOK,directionalPass,setupPass,(outType==='BUY'?closeQualityBuy&&directBuyQuality:outType==='SELL'?closeQualitySell&&directSellQuality:true),validRisk,scorePass],passed=qualityParts.filter(Boolean).length,rejReasons=[];if(!atrOK)rejReasons.push('volatilidade fora da faixa');if(!bodyOK)rejReasons.push('candle de confirmação demasiado fraca');if(!buyTF&&!sellTF)rejReasons.push('menos de 3 timeframes alinhados');if(buyTF&&t1!=='ALTA')rejReasons.push('BUY requer H1 alinhado');if(sellTF&&t1!=='BAIXA')rejReasons.push('SELL requer H1 alinhado');if(!scorePass&&(type==='BUY'||type==='SELL'))rejReasons.push(type==='BUY'?`força BUY ${score} — requer > ${C.buyScoreMin}`:`força SELL ${score} — requer < ${C.sellScoreMax}`);if(outType==='BUY'&&!emaBuy)rejReasons.push('preço abaixo da EMA200');if(outType==='SELL'&&!emaSell)rejReasons.push('preço acima da EMA200');if(outType==='BUY'&&!extensionBuy)rejReasons.push('BUY demasiado esticado da EMA20');if(outType==='SELL'&&!extensionSell)rejReasons.push('SELL demasiado esticado da EMA20');if(!buyRSI&&!sellRSI)rejReasons.push('RSI sem momentum direcional');if(!setupPass)rejReasons.push('sem confirmação estrutural BOS/CHOCH ou breakout/reteste');if(tacticalBuy&&(!closeQualityBuy||!directBuyQuality))rejReasons.push('breakout BUY sem força/fecho suficiente');if(tacticalSell&&(!closeQualitySell||!directSellQuality))rejReasons.push('breakdown SELL sem força/fecho suficiente');if(bothSetup)rejReasons.push('BUY e SELL simultaneamente — sinal ambíguo');if(!validRisk)rejReasons.push(Number.isFinite(riskAtr)&&riskAtr<C.minRiskAtr?`stop demasiado curto: risco ${riskAtr.toFixed(2)} ATR — requer ≥ ${C.minRiskAtr.toFixed(2)} ATR`:'sem risco/TP1 válido');const formingR=rsi(main.map(x=>x.close)),tol=A*C.zoneAtr,armedBuy=!!tacticalBuy||((forming.high>=S.resistance-tol)&&(forming.close<=S.resistance+A*C.retestAtr)),armedSell=!!tacticalSell||((forming.low<=S.support+tol)&&(forming.close>=S.support-A*C.retestAtr)),armed=armedBuy&&!armedSell?'BUY':armedSell&&!armedBuy?'SELL':null;const reasons=outType==='BUY'?[tacticalBuy,'3+ timeframes alinhados','força > 60','preço acima da EMA200','RSI > 50','fecho do M1 com força','confirmação estrutural','TP1 1:1',tp2Eligible?'TP2 1:2 por força':'TP2 só se houver força']:outType==='SELL'?[tacticalSell,'3+ timeframes alinhados','força < 40','preço abaixo da EMA200','RSI < 50','fecho do M1 com força','confirmação estrutural','TP1 1:1',tp2Eligible?'TP2 1:2 por força':'TP2 só se houver força']:[];return{type:outType,score,confidence:outType==='WAIT'?0:score,price:entry,signalTimestamp:sig.timestamp||null,rsi:R,ema20:e20,ema50:e50,ema200:e200,atr:A,support:S.support,resistance:S.resistance,trend5:t5,trend15:t15,trend1:t1,sl:sl==null?null:round(sl),tp:tp1==null?null:round(tp1),tp1:tp1==null?null:round(tp1),tp2:tp2==null?null:round(tp2),rr:rr==null?null:rr,rr1:rr1==null?null:rr1,rr2:rr2==null?null:rr2,tp2Eligible,reasons,rejectionReasons:rejReasons,setupState:armed?'ARMADO':'SEM SETUP',armedDirection:armed,formingPrice:forming.close,formingRsi:Number.isFinite(formingR)?formingR:null,tactical:{buy:tacticalBuy,sell:tacticalSell,buyLevel:tb.level,sellLevel:ts.level,buyEvent:tb.event,sellEvent:ts.event,buyRetestIndex:tb.retestIndex,sellRetestIndex:ts.retestIndex,buyConfirmationIndex:tb.confirmationIndex,sellConfirmationIndex:ts.confirmationIndex,bosBuy:ms.bosBuy,chochBuy:ms.chochBuy,bosSell:ms.bosSell,chochSell:ms.chochSell,buyStructuralConfirm,sellStructuralConfirm},source:'candles reais fechados',filters:{marketIsTradable:atrOK,bodyIsDecisive:bodyOK,buyTrend:buyTF,sellTrend:sellTF,ema200Buy:emaBuy,ema200Sell:emaSell,extensionBuy,extensionSell,buyBreakout:tb.direct&&directBuyQuality,sellBreakdown:ts.direct&&directSellQuality,buyPullback:tb.retest,sellRetest:ts.retest,buyContinuation:tb.continuation,sellContinuation:ts.continuation,reversalBuy:tb.near&&closeQualityBuy,reversalSell:ts.near&&closeQualitySell,bosBuy:ms.bosBuy,bosSell:ms.bosSell,chochBuy:ms.chochBuy,chochSell:ms.chochSell,bosConfirmed:bosBuyConfirm,chochConfirmed:ms.chochBuy||ms.chochSell,structuralConfirmBuy:buyStructuralConfirm,structuralConfirmSell:sellStructuralConfirm,technicalPass:atrOK&&bodyOK,m5Buy:t5==='ALTA',m5Sell:t5==='BAIXA',m15Buy:t15==='ALTA',m15Sell:t15==='BAIXA',h1Buy:t1==='ALTA',h1Sell:t1==='BAIXA',closeQualityBuy,closeQualitySell,directBreakQualityBuy:directBuyQuality,directBreakQualitySell:directSellQuality,tp2Eligible,score,scorePass,buyScorePass:score>C.buyScoreMin,sellScorePass:score<C.sellScoreMax}};
}
root.RBGoldSniper=Object.assign(root.RBGoldSniper||{},{CONFIG:C,sma,ema,rsi,atr,trend,priorStructure:structure,structureSignal,analyze});
})(globalThis);
