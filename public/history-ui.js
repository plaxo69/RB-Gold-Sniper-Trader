(()=>{const KEY='rb_gold_sniper_signals_v1';let activeAsset='XAUUSD';const $=id=>document.getElementById(id);const money=v=>Number.isFinite(+v)?`$${(+v).toFixed(2)}`:'—';function todayStart(){const d=new Date();d.setHours(0,0,0,0);return d.getTime()}function load(){try{const raw=JSON.parse(localStorage.getItem(activeAsset==='XAUUSD'?KEY:`${KEY}_${activeAsset}`)||'[]');if(!Array.isArray(raw))return[];return raw.filter(x=>x&&['BUY','SELL'].includes(x.type))}catch{return[]}}function renderHistory(){const list=$('history-list'),stats=$('history-stats');if(!list)return;const h=load().filter(x=>x&&['BUY','SELL'].includes(x.type)).sort((a,b)=>{const ta=Date.parse(a.signalTimestamp||a.time||0)||0,tb=Date.parse(b.signalTimestamp||b.time||0)||0;return tb-ta});const wins=h.filter(x=>x.status==='WIN').length,losses=h.filter(x=>x.status==='LOSS').length,exp=h.filter(x=>x.status==='EXPIRADA').length,open=h.filter(x=>x.status==='ALERTA').length;if(stats)stats.textContent=`${h.length} sinais • ${wins} WIN • ${losses} LOSS • ${exp} EXP • ${open} abertos`;if(!h.length){list.innerHTML='<div class="history-empty">Ainda não existem sinais no histórico de hoje.</div>';return}list.innerHTML=h.map(t=>{const status=t.status||'ALERTA',cls=status==='WIN'?'history-win':status==='LOSS'?'history-loss':status==='EXPIRADA'?'history-exp':'history-open',dir=t.type==='BUY'?'📈 COMPRA':'📉 VENDA',dc=t.type==='BUY'?'history-buy':'history-sell',when=new Date(t.signalTimestamp||t.time||Date.now()),date=Number.isNaN(when.getTime())?'—':when.toLocaleString('pt-PT'),reason=t.outcomeReason||((t.reasons||[]).join(' • '));return `<div class="history-card"><div class="history-card-top"><span class="${dc}">${dir}</span><span class="${cls}">${status}</span></div><div class="history-card-details"><span>${date}</span><span>Entrada ${money(t.price)}</span><span>TP ${money(t.tp)}</span><span>SL ${money(t.sl)}</span><span>RR 1:${Number(t.rr||0).toFixed(2)}</span></div>${reason?`<div class="history-card-reason">${reason}</div>`:''}</div>`}).join('')}document.addEventListener('DOMContentLoaded',()=>{renderHistory();setInterval(renderHistory,2000);window.addEventListener('rb-history-updated',renderHistory);window.addEventListener('rb-asset-changed',e=>{activeAsset=e.detail?.asset||'XAUUSD';renderHistory()})});})();